import CounterWrapper from './style';

const FcCounterWrapper = ({ children }) => {
    return (
        <CounterWrapper>
            {children}
        </CounterWrapper>
    )
};

export default FcCounterWrapper;
